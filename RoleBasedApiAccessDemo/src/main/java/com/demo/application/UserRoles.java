package com.demo.application;

public class UserRoles {
	//Using class instead of enum as enum are hard to pass in annotations and need to be constant value anyways
        public static final String ADMIN_VALUE = "ADMIN";
        public static final String STAFF_VALUE = "STAFF";
}
